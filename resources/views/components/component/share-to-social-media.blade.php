<div class="mb-5">
    <h6 class="fw-bold text-muted mb-3">Bagikan Berita Ini:</h6>
    <div class="share-buttons">
        <a href="{{ $url ?? '#' }}" class="btn btn-primary"><i class="bi bi-facebook"></i></a>
        <a href="{{ $url ?? '#' }}" class="btn btn-info"><i class="bi bi-twitter-x"></i></a>
        <a href="{{ $url ?? '#' }}" class="btn btn-success"><i class="bi bi-whatsapp"></i></a>
        <a href="{{ $url ?? '#' }}" class="btn btn-danger"><i class="bi bi-envelope-fill"></i></a>
    </div>
</div>
